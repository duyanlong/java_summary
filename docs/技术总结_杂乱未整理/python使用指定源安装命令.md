# python 升级到 3.7 以后执行 pip报错   可以通过-i指定源 解决错误 Can't connect to HTTPS URL because the SSL module is not available
pip3.9 search -i https://mirrors.aliyun.com/pypi/simple/ mysql
# -i https://pypi.tuna.tsinghua.edu.cn/simple